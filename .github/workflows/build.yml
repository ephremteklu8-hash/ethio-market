name: Build Ethio Market APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Fix project name
        run: |
          sed -i 's/^name:.*/name: ethio_market/' pubspec.yaml

      - name: Create fresh Flutter Android project
        run: |
          rm -rf android
          flutter create --project-name ethio_market --platforms=android .

      - name: Set Android package name
        run: |
          python3 - <<'PY'
          from pathlib import Path

          old = "com.example.ethio_market"
          new = "com.ethiomarket.app"

          for path in Path("android").rglob("*"):
              if path.is_file():
                  try:
                      text = path.read_text()
                      if old in text:
                          path.write_text(text.replace(old, new))
                  except (UnicodeDecodeError, OSError):
                      pass
          PY

      - name: Add Firebase configuration
        env:
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_CONFIG }}
        run: |
          printf '%s' "$FIREBASE_CONFIG" > android/app/google-services.json

      - name: Configure Google Services
        run: |
          python3 - <<'PY'
          from pathlib import Path

          settings = Path("android/settings.gradle.kts")
          text = settings.read_text()

          if 'com.google.gms.google-services' not in text:
              text = text.replace(
                  'id("org.jetbrains.kotlin.android") version',
                  'id("com.google.gms.google-services") version "4.4.3" apply false\n    id("org.jetbrains.kotlin.android") version'
              )

          settings.write_text(text)

          app = Path("android/app/build.gradle.kts")
          text = app.read_text()

          if 'id("com.google.gms.google-services")' not in text:
              text = text.replace(
                  'id("org.jetbrains.kotlin.android")',
                  'id("org.jetbrains.kotlin.android")\n    id("com.google.gms.google-services")'
              )

          app.write_text(text)
          PY

      - name: Verify Firebase package
        run: |
          python3 - <<'PY'
          import json

          with open("android/app/google-services.json") as f:
              data = json.load(f)

          package_name = data["client"][0]["client_info"]["android_client_info"]["package_name"]

          print("Firebase package:", package_name)

          if package_name != "com.ethiomarket.app":
              raise Exception(
                  "Firebase package must be com.ethiomarket.app"
              )
          PY

      - name: Get packages
        run: flutter pub get

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ethio-market-apk
          path: build/app/outputs/flutter-apk/app-release.apk
